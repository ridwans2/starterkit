*.log
.DS_Store
.env
.env.backup
.env.production
/database/*.sqlite
/database/*.sqlite-shm
/database/*.sqlite-wal
/build/coverage
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/media-library
/storage/pail
/tests/Browser/Screenshots
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Sub-agent worktrees: they carry a copy of vendor/ and node_modules — never commit
/.claude/worktrees/

# Agent orchestration artifacts: the captured requirement (the copy that counts lives
# in each wiki's `00-requisito.md`), the worktree setup script and Playwright MCP
# snapshots. They are process residue, not part of the project.
/.claude/requisitos/
/.claude/setup-worktree.sh
/.playwright-mcp/
.solomd/

# Phase scratch (migrations copied with `git archive HEAD` to simulate an old
# production database, see the English-first plan): verification residue, never the project's.
/.raja/
